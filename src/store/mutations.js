export function setCurrentIndex(state, index) {
  state.currentIndex = index;
}
export function setSequenceList(state, list) {
  state.sequenceList = list;
}
export function setPlaylist(state, list) {
  state.playlist = list;
}
export function setPlayState(state, playState) {
  state.playing = playState;
}
export function setFullScreen(state, fullScreen) {
  state.fullScreen = fullScreen;
}
export function setPlayMode(state, playMode) {
  state.playMode = playMode;
}
export function setFavouriteList(state, favouriteList) {
  state.favouriteList = favouriteList;
}
